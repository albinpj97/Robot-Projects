tasks:
  Run all tasks:
    shell: python -m robot --report NONE --outputdir output --logtitle "Task log" tasks.robot
  
  Run_Orange_HR_Process:
    robotTaskName: Orange HR Process

condaConfigFile: conda.yaml
artifactsDir: output
PATH:
  - .
PYTHONPATH:
  - keywords
  - libraries
  - variables
  
ignoreFiles:
  - .gitignore
